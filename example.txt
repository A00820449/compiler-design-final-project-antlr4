program a;
var int: a,b; float: f;
function int: fun(float x, float y);
var float: z;
{
    print("hello\n");
    return(-1.0);
}
main();
var bool: b;
{
    if (b > true) 
    {
       fun(1+1, b - (-c));
    };
    input(a);
    a = 1 * (2 + 3 * 1.0 )^7 / -2 + 1 > 1;
    b = -1 - -(-1);
    while(true & false | a) 
    {/*comment*/
        
    };
}